# Confidential Second Brain content — never push to the public repo
BRAIN.md
*.docx
.DS_Store

# Local drift-detection cache — machine-local baseline, not shared
.brain-state.json

# Installed-vault artifacts — this repo is the clean installer package, not an instance
CLAUDE.md
chat-history/
docs/
